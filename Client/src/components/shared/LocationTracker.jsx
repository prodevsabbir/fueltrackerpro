import React from 'react';

// Location broadcasting is handled entirely by the Map page's
// stable socket connection (socketRef). This component is kept
// as a no-op to avoid breaking imports in the root layout.
const LocationTracker = () => null;

export default LocationTracker;
